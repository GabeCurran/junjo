# Example: Three.js webgame

Minimal Three.js scene + `@junjo/sdk` integration. Shows the "create guild, invite member, check permission" flow from a browser.

Real example code lands once the SDK has real implementations.
