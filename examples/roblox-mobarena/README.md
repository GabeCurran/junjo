# Example: Roblox MOBArena

Pointer to the dogfood target for `junjo-roblox`. The actual game lives in the separate `mobarena-roblox` repo. This directory will host a copy of the Junjo-integration code (or a `git submodule` link) once that integration exists.

Nothing here yet.
