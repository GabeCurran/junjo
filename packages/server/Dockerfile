# Placeholder. Real Dockerfile lands when the server has real routes.
# Multi-stage build, alpine base, run `node dist/index.js`.
FROM node:20-alpine
WORKDIR /app
CMD ["node", "-e", "console.log('junjo-server placeholder image')"]
